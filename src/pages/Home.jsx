import Layout from "../layout/Layout";
import { TodoListContainer } from "../features/todo";

const Home = () => {
  return (
    <Layout>
      <TodoListContainer />
    </Layout>
  );
};

export default Home;
